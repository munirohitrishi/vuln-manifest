<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.vuln-lab</groupId>
    <artifactId>java-vuln-app</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>
    <name>Vulnerable Java App - SCA Practice</name>

    <!-- ============================================ -->
    <!--  INTENTIONALLY VULNERABLE DEPENDENCIES       -->
    <!--  DO NOT USE IN PRODUCTION!                   -->
    <!-- ============================================ -->

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.6.0</version> <!-- VULNERABLE: Multiple CVEs in Spring Boot 2.6.0 -->
    </parent>

    <properties>
        <java.version>11</java.version>
    </properties>

    <dependencies>
        <!-- VULN: Spring Boot Web 2.6.0 - Multiple CVEs including Spring4Shell transitives -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- VULN: Log4j 2.14.1 - CVE-2021-44228 (Log4Shell) CVSS 10.0 -->
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
            <version>2.14.1</version>
        </dependency>

        <!-- VULN: Jackson Databind 2.13.0 - Multiple deserialization CVEs -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>2.13.0</version>
        </dependency>

        <!-- VULN: Apache Commons Text 1.9 - CVE-2022-42889 (Text4Shell) CVSS 9.8 -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-text</artifactId>
            <version>1.9</version>
        </dependency>

        <!-- VULN: SnakeYAML 1.30 - CVE-2022-1471 (RCE via deserialization) CVSS 9.8 -->
        <dependency>
            <groupId>org.yaml</groupId>
            <artifactId>snakeyaml</artifactId>
            <version>1.30</version>
        </dependency>

        <!-- VULN: Apache Commons Collections 3.2.1 - Deserialization gadget chain -->
        <dependency>
            <groupId>commons-collections</groupId>
            <artifactId>commons-collections</artifactId>
            <version>3.2.1</version>
        </dependency>

        <!-- VULN: Hibernate 5.4.0 - Multiple CVEs -->
        <dependency>
            <groupId>org.hibernate</groupId>
            <artifactId>hibernate-core</artifactId>
            <version>5.4.0.Final</version>
        </dependency>

        <!-- Test dependency (lower priority for triage) -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.12</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
